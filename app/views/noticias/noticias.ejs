<!DOCTYPE html>
	<html lang="pt-br">
		<head>
			<meta charset="utf-8"/>
			<title>Portal de not�cias</title>

			<style>
				body{
					display: flex;
					justify-content: center;
					align-items: center;
					height: 100vh;
					background-color: #f4f4f4;
					flex-direction: column;
				}
			</style>
		</head>
	<body>

		<h1>Not�cia</h1>

<!-- 
		<input type="text" id="search" placeholder="Pesquisar notícia...">
		<button onclick="buscarNoticias()">buscar termo</button> -->

		<div id="resultados"></div>

		<table>
			<tr>
				<td>ID</td>
				<td>Titulo</td>
				<td>Noticia</td>
			</tr>
			<% for (var i = 0; i < noticias.length; i++) {%>
			<tr>
				<td><%= noticias[i].id_noticia %></td>
				<td><%= noticias[i].titulo  %></td>
				<td><%= noticias[i].noticia  %></td>
			</tr>
			<% } %>

			<a href="/">home</a>
		</table>
	
	<script>

//está dando erro nesse script.. não reconhece como valor json e sim como html Erro ao buscar noticia:  SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
		// function buscarNoticias() {
		// 	let termo = document.getElementById('search').value;

		// 	fetch(`noticias?termo=${encodeURIComponent(termo)}`)
		// 		.then(response => response.json())
		// 		.then(data => {
		// 			var resultados = document.getElementById('resultados');
		// 			// resultados.innerHTML = '';

		// 			data.forEach(noticias => {
						
		// 				// document.getElementById("resultados").innerHTML = `<h3>${noticias.Titulo}</h3>`;
						
		// 				var div = document.createElement('div');
		// 				div.innerHTML = `<h3>${noticias.Titulo}</h3>`;//<p>${noticia.noticia}</p>
		// 				resultados.appendChild(div);
		// 			});
		// 		})
		// 		.catch(error => console.error('Erro ao buscar noticia: ', error));
		// }

	</script>
	</body>
</html>